# ── Assistify web — environment template ────────────────────────────
# Copy to `.env`. Vite only exposes variables prefixed with VITE_.

# Backend API base URL. Must match the server's PORT (server/.env → 5050)
# and include the /api/v1 prefix.
VITE_API_URL=http://localhost:5050/api/v1
